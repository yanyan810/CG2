name: DebugBuild

on:
  push:
    branches:
      - master

env:
  # リポジトリのルートディレクトリを起点としたソリューションファイルのパス
  SOLUTION_FILE_PATH: project/CG2_SetUp.sln
  # Visual Studio の構成(Debug/Release)
  CONFIGURATION: Debug

jobs:
   build:
     runs-on: windows-2022
     steps:
       - name: Checkout
         uses: actions/checkout@v4
       - name: Add MSBuild to PATH
         uses: microsoft/stepup-msbuild@v2
         with:
           msbuild-architecture: x64
       - name: Build
         run: 
           masbuild ${{env.SOLUTION_FILE_PATH}} /p:Platform=x64,Configuration=${{env.CONFIGURATION}}
           
